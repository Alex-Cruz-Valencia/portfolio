// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // TODO: replace with the real production URL once the Vercel domain is confirmed.
  // Used for canonical + Open Graph URLs.
  site: 'https://portfolio-alex-cruz-valencia.vercel.app',
});
